#!/usr/bin/env bash
# displays dns info

domain=$1
sub=${2:-"www lb-01 web-01 web-02"}

f(){ dig "$1.$domain" | awk '/\tIN\tA/ {print "The subdomain " sub " is a A record and points to " $5}' sub="$1"; }

for i in $sub; do f "$i"; done
